

  <h1>{{title }}</h1>
  <p>Welcome to {{title }}</p>

  <div>
      <h1>yo these are my friends</h1>
  {{#each friends}}
      <p>{{ friendId}}</p>
    <p>{{ User.userName}}</p>
    {{/each}}
  </div>
